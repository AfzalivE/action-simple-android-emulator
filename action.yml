name: 'Simple Android Emulator'
description: 'Simple Android Emulator'
runs:
  using: "composite"
  steps:
    - run: echo Hello
      shell: bash
    - run: ${{ github.action_path }}/publish-report.sh
      shell: bash
